# AGENTS.md — 给AI编码助手（Codex/Claude等）的项目说明

这是一个 **A股短线情绪复盘系统** 的发布仓库。每天由主力机自动镜像推送（19:00），你在这里看到的永远是当天复盘后的最新全量材料。

## ⚠️ 最重要的一条规则：本仓库是"单向镜像"

主力机每天用 robocopy /MIR **镜像覆盖** `市场数据/` 目录和根目录网页后强制推送。这意味着：

- **禁止修改** `市场数据/`、`archive/`、根目录 `*.html`、`skill/`、`米开朗基瑞/` 里的任何文件后push——第二天会被主力机镜像**无声冲掉**，你的提交等于白做。
- 你的正确工作方式：**只读这些目录**。要写代码、跑回测、出分析报告，请在仓库里新建自己的目录（如 `codex_workspace/`、`analysis/`），或者干脆在仓库外干活。新建的独立目录不在镜像范围内，不会被冲掉。
- 需要修改系统本体脚本？把改动建议写成文件放进你自己的目录，由用户带回主力机人工合入。

## 这个系统是什么

每日"**环境 → 周期 → 题材 → 情绪**"四层复盘 + 多页盯盘台网页 + 自学习闭环（席位胜率库、集合竞价训练、龙虎榜训练）。方法论地基是"米开朗基瑞"的短线量化套利体系。

## 读文件的顺序（想理解这套体系，按这个顺序读）

1. `市场数据/README_系统运行手册.md` — 系统怎么跑、产出什么
2. `skill/sentiment-review-system/SKILL.md` — 完整方法论：复盘流程、评分规则、多agent架构
3. `米开朗基瑞/米开朗基瑞交易体系手册V2.md` — 方法论地基（情绪周期、题材、龙头战法）
4. `米开朗基瑞/米开朗基瑞_量化规则清单.md` — 可量化的规则提炼
5. `市场数据/_agent规格/` + `市场数据/_盯盘台组件规范.md` — 多agent分工与页面规范
6. `市场数据/_评分卡与校准规则.md` — 荐票评分与自我校准

## 目录速查

| 路径 | 内容 | 可否修改 |
|---|---|---|
| `市场数据/复盘/YYYYMMDD.md/html` | 每日复盘正文 | 只读 |
| `市场数据/复盘/盯盘台/` | 盯盘台网页源 | 只读 |
| `市场数据/2026MMDD/` | 每日原始数据快照(JSON/CSV) | 只读 |
| `市场数据/_学习/` | 学习数据：席位库/竞价训练/K线缓存(~112MB) | 只读 |
| `市场数据/*.py` | 全部系统脚本 | 只读(改动走建议文件) |
| `市场数据/_席位胜率库.json` | 龙虎榜席位胜率统计 | 只读 |
| 根目录 `*.html` + `archive/` | GitHub Pages盯盘台站点 | 只读 |
| 你自己新建的目录 | 你的分析/回测/报告 | ✅ 随便写 |

## 跑代码须知

- 脚本默认根路径是 `D:\股票数据\市场数据`（Windows绝对路径）。在别的环境跑，改脚本开头的 `BASE` 行，或把数据放到相同路径。
- 依赖：`pip install -r requirements.txt`（Python 3.10+）。
- 行情数据走网络在线抓（akshare/东财/THS接口），不依赖本地行情软件。
- 每日数据快照里的字段口径以 `市场数据/README_系统运行手册.md` 为准。

## 分析时必须遵守的铁律（用户的最高优先级要求）

1. **零编造**：所有结论必须有数据出处（对应快照文件/接口），拿不到的数据明确说"拿不到"，禁止脑补数字。
2. **A/C档结论分级**：A档=数据直接支持；C档=推断/样本不足，必须标注。
3. **回测零后视镜**：任何回测的进出场信号必须100%纯因果——决策时点只能用当时已知的信息，禁止用事后才确认的信号（如已完成的线段端点）定进场价。
4. **幸存者偏差必标注**：股票池若只含当前在册股票（缺退市股），绝对收益结论必须注明是"幸存者偏差上界"。

## 常见任务示例

- "总结最近一周情绪周期" → 读 `市场数据/复盘/` 近5个交易日的md + 各日快照的市场温度
- "统计某席位胜率" → 读 `市场数据/_席位胜率库.json`（近一月/近三月CSV在 `市场数据/` 根下）
- "验证某个复盘判断" → 用 `市场数据/2026MMDD/` 快照对照次日实际走势，结论分A/C档
- "改进评分卡" → 读 `_评分卡与校准规则.md`，把建议写到你自己的目录，不要直接改
