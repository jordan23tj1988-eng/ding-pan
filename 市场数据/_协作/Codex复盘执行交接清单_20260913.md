# A股情绪复盘系统 · Codex 执行交接清单

> **交接方**：Hermes Agent（A 档案 / 原复盘执行方）
> **承接方**：本机 Codex CLI
> **交接日**：2026-09-13（周日）
> **文档性质**：自包含执行手册。你（Codex）没有本次对话的上下文，**本文件即你的全部背景**。
> **权威性排序**：本文件 < `_链路地图.md`/`AGENTS.md` < 运行时契约真源（第 2 节）< 脚本源码本身。
> 凡本文件与脚本源码冲突，**以源码为准**，并把冲突如实记入你的交付报告。

---

## 0. 一句话任务

每个交易日收盘后，把 `D:\股票数据\市场数据` 生产工作区跑完一整套流水线（取数 → 五路独立判断 → 总审 → 推演 → 台账/结算 → 出页 → 哨兵），
产出 7 个盯盘台页面 + 全部判断 JSON + 模拟盘账本，**并通过全部验收门**。
「跑完」的定义不是脚本退出码 0，而是**第 6 节的验收门全绿**。

---

## 1. 开工前必读（按序，共 6 份）

| # | 文件 | 作用 |
|---|---|---|
| 1 | `D:\股票数据\市场数据\_协作\检查改动.py` 跑一次：`python _协作/检查改动.py check` | 看上一班改了什么 |
| 2 | `D:\股票数据\市场数据\AGENTS.md` | 工作区协作纪律、只读边界、改动流程 |
| 3 | `D:\股票数据\市场数据\_链路地图.md` | **生产链路权威图**（超长行 UTF-8，`read_file` 会误判二进制 → 用 python 折行或 `rg` 读） |
| 4 | `D:\股票数据\市场数据\_变更总账.md` 末尾 30 行 | 最近改动登记 |
| 5 | `D:\股票数据\市场数据\_多agent重构_设计规范.md` + `_盯盘台组件规范.md`（v4.1） | bodies 视觉语言（写页面 HTML 必读） |
| 6 | `D:\股票数据\市场数据\_agent规格\*.md` | 13 份子 agent 规格：`00_总agent` / `07_总审agent` / `08_竞价命门` … `12_涨停复盘` |

**运行时契约真源（最重要）**：每日完整流程的权威描述内嵌在 cron 任务里，不是任何 .md：

```
文件：C:\Users\66353\AppData\Local\hermes\profiles\a\cron\jobs.json
任务：id = c4d23047161f，name = sentiment-daily-review
```

取法（占位符 `{今日}`/`{昨日}` 需替换为实际日期 `YYYYMMDD`）：

```bash
python -c "
import json;d=json.load(open(r'C:\Users\66353\AppData\Local\hermes\profiles\a\cron\jobs.json',encoding='utf-8'))
j=[x for x in d['jobs'] if x['id']=='c4d23047161f'][0]
print(j['prompt'])"
```

★ 该 cron 任务**当前处于 paused 状态**（2026-09-11 16:14 暂停）。**你不要去启用或修改它**，见第 8 节。

---

## 2. 运行环境（硬约束，违反即崩）

### 2.1 解释器

| 项 | 值 |
|---|---|
| **唯一可用解释器** | `D:\股票数据\.venv312\Scripts\python.exe`（Python 3.12.13） |
| 已装依赖 | `iFinDPy`、`akshare` 1.18.84、`pandas`（实测通过） |
| ❌ 禁用 | Hermes 自带 python、系统 Python310/311/312、`.venv312` 以外任何环境 |

**统一调用模板**（每次都必须带 `env -u PYTHONPATH`）：

```bash
cd "D:\股票数据\市场数据" && env -u PYTHONPATH "D:\股票数据\.venv312\Scripts\python.exe" <脚本.py> <参数>
```

- ★ `env -u PYTHONPATH` 是**必须的**：宿主机注入的 `PYTHONPATH` 会让 venv 加载错误的 numpy cp311 → 直接崩溃。
- 本机 MSYS 路径自动转换**已关闭**：bash 内建 `cd /d/...` 可用；但传给 git/python/node/codex 等原生程序的路径参数一律用 `D:/...` 正斜杠形式。
- 中文路径必须加引号。

### 2.2 Codex 自身运行方式

```bash
CODEX=$(ls -d "C:/Users/66353/AppData/Local/OpenAI/Codex/bin"/*/codex.exe 2>/dev/null | head -1)   # 桌面版，不在 PATH
# ★ bin 目录按哈希分版本：有的哈希目录只有 rg.exe，必须选含 codex.exe 的那个；取最新≠ls排序最后
"$CODEX" exec --sandbox danger-full-access --cd <git工作区> "<自包含 brief>"
```

- Codex **必须在 git 仓库内**运行 → 在 `D:\股票数据\codex_workspace\<任务名>\` 执行 `git init`（空仓库即可）。
- 沙箱必须 `danger-full-access`：任务要读写工作区外的 `D:\股票数据\市场数据`。
- 生产目录 `D:\股票数据\市场数据` **本身不是 git 仓库**（实测）——这是正常的，你只把它当**数据/脚本目录**用绝对路径访问，不要在那里 `git init`。

### 2.3 只读边界（**改了也白改，且会坏事**）

| 路径 | 规则 |
|---|---|
| `D:\黄金对照版717\` | **只读基准，永不动**（黄金版视觉锚） |
| 历史日块（`archive\{d}.html`、7/16 及更早的 bodies/页面） | 存档快照，**永不动** |
| `_学习\发出版备份_*`、`_学习\龙虎榜复盘存档`、`_学习\涨停复盘存档` | 历史证据，只读 |
| `C:\Users\66353\AppData\Local\hermes\profiles\a\cron\jobs.json` | 不改（除非用户明确授权） |

### 2.4 数据源（三个源，配额/可用性各不相同）

| 源 | 用途 | 注意 |
|---|---|---|
| **akshare / 东财 EM** | `市场数据下载.py` 取涨停池/跌停池/炸板池/强势池/龙虎榜 | 主力源，稳定 |
| **THS（hithink-finance）** | 独有涨停池/连板天梯/异动/热榜 | 凭据 `%APPDATA%\hithink-finance\credentials.env`；THS 原因缺失时归位标 B 档行业兜底。**THS 无龙虎榜席位** → 席位走东财 |
| **iFinD** | 实时行情、`iwencai` 问句、K线 | 历史K线月度额度会用尽（-4318，跨日不恢复）；实时行情与 iwencai 是独立配额池；失败**如实标注，不许编造** |

---

## 3. 系统全景

### 3.1 架构

```
取数(akshare/THS/iFinD)
  └→ 【五路独立判断】竞价auction / 席位lhb / 题材theme / 产业逻辑logic / 涨停复盘limitup
        └→ 【cycle 周期情绪路】读五路投票定档位
        └→ 【总审】对抗式裁决五路分歧 + 产出「指派清单」
              └→ 【推演】次日可证伪预测（每条带机械化判定条件）
                    └→ 【台账注入 / 五路结算 / 模拟盘六账本】
                          └→ 【出页】7页 HTML → release 门禁 → 到站
                                └→ 【哨兵】一致性检查 C1~C13，FAIL 不算完
```

### 3.2 页面（7 张，缺一即不合格）

| 页面 | 路径（`复盘\盯盘台\`） | 内容 |
|---|---|---|
| 概览 index | `index.html` | Top5 跨路荐票 · 明日核心观察点 · 五路看牌 · 拐点预警 · 总裁决·自主进化 · Master 综合 |
| 周期 cycle | `cycle.html` | 量能台阶 · 先行指标 · 连板梯队 · 周期三态档位 · 周期投票牌 · 我的认知迭代时间线 |
| 竞价 auction | `auction.html` | 竞价命门 |
| 席位 lhb | `lhb.html` | 席位命门（黄金页结构锁） |
| 题材 theme | `theme.html` | 题材命门（★发布期间冻结哈希，见 C13） |
| 逻辑 logic | `logic.html` | 产业逻辑命门（含风险日历踩窗卡） |
| 涨停 limitup | `limitup.html` | 涨停复盘 / 质量命门 |

本地预览（实测在跑）：

```
http://127.0.0.1:8899/              → D:\股票数据\市场数据\复盘\
http://127.0.0.1:8899/%E7%9B%AF%E7%9B%98%E5%8F%B0/index.html   → 盯盘台首页
```
（中文路径须 URL 编码，否则链接显示成回形针）

### 3.3 关键产物路径表

| 类别 | 路径（均相对 `D:\股票数据\市场数据`） |
|---|---|
| 当日行情数据 | `{今日}\zt_pool.csv` `dt_pool.csv` `zb_pool.csv` `strong_pool.csv` `lhb.csv` `summary.json` |
| 五路判断 | `_学习\{route}判断_{今日}.json` + `_学习\{route}_body_{今日}.html` |
| 题材路专属 | `_学习\题材归位_{今日}.json`、`_题材四维.json`、`涨停对链条_{今日}.json`、`题材荐票_{今日}.json`、`题材龙头判断_{今日}.json`、`题材生命周期判断_{今日}.json` |
| 总审 | `_学习\总审_{今日}.json`（**必须含「指派清单」**） |
| 推演 | `_学习\推演_{今日}.json` |
| 交易计划（六路） | `_学习\交易计划_{route}_{今日}.json`，route ∈ auction/lhb/theme/logic/limitup/master |
| 认知库 | `_学习\_认知库_{route}.json` |
| 战绩画像 | `_学习\子agent增强\战绩画像_{route}_{今日}.json` + `_战绩画像汇总_{今日}.json` |
| fact 快照 | `_学习\fact_{今日}.json` |
| judgment（bodies 注入目标） | `_学习\judgment_{今日}.json` |
| 运行状态 | `_学习\_运行状态\{route}_{今日}.json` |
| 模拟盘 | `_学习\_模拟盘\{route}\{状态.json, 净值.json, 账本.jsonl, 看板_{今日}.html}` |
| 归档 | `复盘\盯盘台\archive\{今日}.html` |
| 盘中作战台 | `盘中\{今日}\warboard.json` → `复盘\盯盘台\intraday.html` |

**数字真源铁律**（写任何文案引用涨停数/温度/最高板/票名+板数时）：

- `_学习\_市场温度表.json` → 涨停数 / 温度 / 最高板
- `_学习\_ths_zt_pool.json` → `high_days` 的 "N天M板"（连续板 = 天==板）
- **禁止**从历史日块或记忆抄数字 → 哨兵会机器拦截

---

## 4. 每日执行流水线（15 步）

> 占位符：`{今日}` = 目标复盘日 `YYYYMMDD`，`{昨日}` = 上一交易日。
> 昨日算法（勿用自然日相减）：
> ```bash
> cd "D:\股票数据\市场数据" && env -u PYTHONPATH "D:\股票数据\.venv312\Scripts\python.exe" -c "import json;t=json.load(open('_学习/_市场温度表.json',encoding='utf-8'));ks=sorted(t);d=[x for x in ks if x<'{今日}'];print(d[-1])"
> ```
> ★ **顺序不可乱**：生成器重跑会冲掉台账注入和五路判断注入。带 ★ 的步骤是踩过事故的。

| 步 | 动作 | 命令（省略公共前缀 `cd "D:\股票数据\市场数据" && env -u PYTHONPATH "D:\股票数据\.venv312\Scripts\python.exe"`） | 幂等 |
|---|---|---|---|
| 1 | 取数建当日目录 | `市场数据下载.py {今日}` | 已存在可跳过 |
| 2 | 先行指标补齐 | 检查 `_学习\_情绪先行指标.json` 含当日；缺则补 → `情绪先行指标.py --card {今日}` | 是 |
| 3 | ★题材归位（唯一真源） | 写 `_学习\题材归位_{今日}.json`（**agent 必做**，全涨停逐只） → `涨停对链条.py {今日}` → `题材四维.py {今日}` → `主流题材6有.py {今日}` | 是 |
| 4a | ★五路 T+1 结算 | `质量荐票结算.py {昨日}` → `题材荐票结算.py {昨日}` → `逻辑荐票结算.py {昨日}` → `席位荐票结算.py {昨日}`（**质量先跑=全场均收基准**） | 是 |
| 4a2 | 推演-校准回路 | `预判结算.py {昨日}` → `预判结算.py --preread {昨日}` | 是 |
| 4b–4f | 温度/质量/竞价 | `市场温度.py {今日}` → `涨停质量训练.py --fetch {今日}` → `涨停质量荐票.py {今日}` → `市场温度.py --card {今日}` → `竞价池结算.py {昨日}` → `竞价评分.py {今日}` | 是 |
| 4g–4j | 席位/资金 | `席位动向库.py rank` → `席位荐票.py {今日}` → `资金温度.py` | 是 |
| 4k | P1/P2 数据面 | `中报预增雷达.py {今日}` → `龙虎榜双源校验.py {今日}` → `风险日历.py {今日}` → `竞价撤单差分.py {今日}` → `开盘验证维.py {今日}` → `日内温度曲线.py {今日}` → `日内轮动图谱.py {今日}` | 是 |
| 4l–4n | 扫描/画像 | `横切面扫描.py {今日}` → `自主拓展扫描.py scan {今日}` → `自主拓展扫描.py tick {今日}` → `五路战绩画像.py {今日}` | 是 |
| 4o–4q | ⚠ 脚本**本机不存在** | `认知库打包.py` / `领域手册.py` / `作战包.py` **均已缺失（实测）** → 跳过并在报告注明；改用 `_学习\_认知库_{route}.json`、`_agent规格\0X_*.md` 替代 | — |
| 5 | ★建 judgment 骨架 | 先建再跑注入类脚本，否则注进旧文件（骨架含 `date/更新label/一句话/ticker/bodies{}/archive_body`） | 否，先建 |
| 6 | ★五路独立判断 | **见第 5 节「五路替代方案」** | — |
| 7 | ★总审（独立 spawn，对抗） | 读五路产物 + `_战绩画像汇总_{今日}.json` + 当日数据 → 按 `_agent规格\07_总审agent.md` 写 `_学习\总审_{今日}.json`，**必含** `指派清单` `[{指派ID,指派给,深挖任务,理由,截止,状态}]`（无指派写 `[]` 并写明理由，字段名固定不得改别名） | — |
| 7.5 | ★推演（深度推理） | 读五路 + 总审 + judgment + summary + 先行指标 → `_学习\推演_{今日}.json`，`次日可证伪预测` **≥5 条，每条必须带判定条件** | — |
| 7.6 | playbook 预案单 | `playbook生成.py {今日}` → 策略子 agent 只更新 `buys/sells/watch/cash_pct/notes` → 写 `盘中\{次日}\playbook.json`（★`route_watchlist` 字段不得改；★watch **禁止**含当日竞价复盘票） | ★发出版不可覆盖 |
| 8 | ★自主拓展应答 | 逐项应答 → `_学习\自主拓展应答_{今日}.json`（立项/并入/不深挖 + 可证伪理由）★哨兵 C24 依赖 | 是 |
| 9 | ★五路判断注入 | 逐路校验 schema（荐票标的**必须 ∈ 当日 zt_pool**）→ 把 `_学习\{route}_body_{今日}.html` 注入 `judgment_{今日}.json` 的 `bodies[{route}]` | — |
| 9b | fact 事实快照 | `涨停fact.py {今日}` → `_学习\fact_{今日}.json`（源冲突/缺失 → exit 1 点名） | 是 |
| 9c | v4 影子重放 | ⚠ `D:\股票数据\sentiment_review_v4` **目录不存在（实测）** → 据实报错跳过，**不许硬造** | — |
| 10 | ★台账三件套注入 | `涨停复盘台账.py {今日} --from-data` → `龙虎榜台账.py {今日} --from-data` → `竞价池结算归档.py --inject` | — |
| 11 | 认知迭代全量更新 | `认知迭代全量更新.py {今日}` | 是 |
| 11b | ★周期投票 | `周期投票.py settle {今日}` → 五路各写 `周期投票_{route}_{今日}.json`（`direction` 只许「升/平/降」枚举）→ `周期投票.py tally` → `周期投票.py board` | 是 |
| 11c | 认知写作纪律 | 当日认知**只能**写进 cycle 页「七 我的认知迭代」时间线**顶部**（最新在上）；写错位置会被哨兵 C6 拦截 | — |
| 12 | 第七账结算 | `盘中账本结算.py settle {今日}`（幂等 done 守卫） | 是 |
| 12c | ★概览 Top5 荐票卡 | `跨路荐票.py {今日}` → `_学习\跨路荐票_{今日}.json` + `跨路荐票卡_{今日}.html`。**必须在第 13 步之前跑**，否则概览荐票段为空 | 是 |
| 13 | ★出页 | `生成盯盘台.py {今日}`（内置模拟盘 inject + 六页 PAPERTRADE 自检） | **走发布门禁** |
| 13b | 盘中作战台重建 | `warboard_build.py {今日}` → 重渲 `intraday.html`；验证 `intraday.html` mtime 晚于本次运行 | 幂等，`--force` 可重跑 |
| 14 | ★出页检查 | 见第 6 节四道门 | — |
| 14b | master 结算 | `master结算.py {昨日}` → `_master结算.jsonl` | 是 |
| 15 | `latest.html` 校验 | 若不是跳转页，改为跳转到 `盯盘台/index.html` | — |

**任一步失败 → 报错终止，不要硬闯下一步。**

---

## 5. ★ 五路独立判断的替代方案（本交接最关键的工程决定）

原流水线第 6/7/7.5/7.6b/8 步依赖宿主 `delegate_task` 拉起**独立上下文子 agent**。**Codex 没有这个工具。**

替代原则：**独立性 = 上下文隔离，不是工具名字**。

推荐做法（每个需要独立判断的单元一个 brief 文件 + 一次 `codex exec`）：

```bash
# 工作区已 git init；为每一路写一个自包含 brief 文件
"$CODEX" exec --sandbox danger-full-access --cd "D:/股票数据/codex_workspace/<任务名>" \
  "$(cat briefs/auction.md)"
```

**每路 brief 必须内联（子进程看不到父上下文）**：

1. **身份与任务**：你是「竞价命门」判断员，目标日 `{今日}`，输出路径 `D:\股票数据\市场数据\_学习\auction判断_{今日}.json` + `auction_body_{今日}.html`
2. **输入文件表**（绝对路径，逐条列全）
3. **必读五件套**：
   - `_学习\子agent增强\战绩画像_{route}_{今日}.json`（含环境档拆解；陷阱环境降级）
   - `_学习\_认知库_{route}.json`
   - `_学习\子agent增强\认知库_{route}_{今日}.json`（若存在）
   - `_学习\总审_{昨日}.json` ★ 指派承接源：`指派给=本路 且 截止≤{今日}` 的**必须承接**，第五段写 `承接Master指派[ID]: 深挖任务 → 深挖结果:{结论, 带可结算判定条件}`；无指派须显式声明「无指派可承接」，**不许沉默**
4. **方法论底座**：`_agent规格\0X_XX命门agent.md` 对应文件（`08_竞价命门` / `09_席位命门` / `10_题材命门` / `11_产业逻辑` / `12_涨停复盘`）
5. **荐票纪律**：对照战绩画像荐票（理由写「同类历史 n=.. 胜率..% 均收..%」）；空仓合法；类型诚实（荐票|观察，荐票 ≤3 只）；认知引用必须当日数据验证
6. **负面过滤**：lhb 路读 `龙虎榜双源校验_{今日}.json`；logic 路读 `风险日历_{今日}.json` + `中报预增雷达_{今日}.json` 的双源交叉字段
7. **输出 schema**：
   ```json
   {"日期":"","路":"",
    "判断":{"结论":"","证据":[],"档位":"","置信度":0,"可证伪条件":"","独立盲区声明":""},
    "荐票":{"结论":"","标的":[{"代码":"","名称":"","类型":"","历史对照":"","理由":""}]},
    "认知迭代":[], "深挖":{}}
   ```
8. **★交易计划（契约⑪，硬要求）**：每路必须写 `_学习\交易计划_{route}_{今日}.json`：
   ```json
   {"日期":"","路":"","buys":[{"代码":"","名称":"","权重":0,"理由":"","buy_gate":{"min_gap_pct":0,"max_gap_pct":0}}],
    "sells":[{"代码":"","腿":"open|close","理由":"","sell_switch":""}],"notes":""}
   ```
   `buys` 0~5 只；空仓合法**但必须写理由**；读 `_学习\_模拟盘\{route}\状态.json` **在持票逐票表态**（卖单或 notes 持有理由，**禁漏票**）；**发出版不可覆盖**。
   ⚠ 历史事故：8/13 起六路交易计划断供 5 日，引擎读不到时**静默空仓无任何告警** → 哨兵 C11 专查此项。
9. **铁律**：零后视镜（只用 ≤`{今日}` 数据）/ 零编造 / 中文 / UTF-8 / 独立判断（**不许看他路的判断文件**）
10. **主动查证**：需要补数据时用 iFinD iwencai 问句，**每路每次 ≤3 次**，结果必须引用进证据，失败如实标注

**周期路（cycle）**额外：读五路投票 → 写 `周期主判_{今日}.json` + cycle 七段 body。

**A/C 档分治**：有数据支撑的结论标 `[A]`，纯推断标 `[C]`，不得混。

---

## 6. 验收门（全过才算完成）

### 门 1 · 专属核对（4 个，按数据口径分）

```bash
env -u PYTHONPATH "D:\股票数据\.venv312\Scripts\python.exe" limitup数据核对.py {今日}
env -u PYTHONPATH "D:\股票数据\.venv312\Scripts\python.exe" theme数据核对.py  {今日}
env -u PYTHONPATH "D:\股票数据\.venv312\Scripts\python.exe" cycle数据核对.py  {今日}
env -u PYTHONPATH "D:\股票数据\.venv312\Scripts\python.exe" lhb数据核对.py    {今日}
env -u PYTHONPATH "D:\股票数据\.venv312\Scripts\python.exe" auction数据核对.py {今日}
# 以上均支持 --page <文件名> 指定页面
```

### 门 2 · 一致性哨兵（**C1~C13，rc=0 才算过**）

```bash
env -u PYTHONPATH "D:\股票数据\.venv312\Scripts\python.exe" 复盘一致性哨兵.py {今日}
```

实测检查项（源码 `复盘一致性哨兵.py` 第 4–16 行）：

| 项 | 查什么 |
|---|---|
| C1 | 页面残留缺口叙述词（不可达/未刷新/溢价 null/结算 None/数据缺口待补/挂起未重训） |
| C2 | PAPERTRADE 头部累计% == `_模拟盘\{route}\状态.json`（**数字必须出自引擎**） |
| C3 | 看板 `看板_{d}.html` mtime ≥ bars_cache 最新 mtime（旧价看板事故根因） |
| C4 | bars_cache 当日覆盖率（涨停池 + 荐票股 + 持仓股必须 100%） |
| C5 | 昨日结算字段非全 None（`c5_expired` 未到期则跳过） |
| C6 | HTML 结构完好 + 时间线折叠 + div 开闭配对 + 游离 `>` + `rowA` 直接子元素 = `[hero,kpi,kpi,kpi,kpi]`（cycle 例外） |
| C7 | cycle.html 当日 `涨停数_净` 必须出现（模块化渲染异常拦截） |
| C8 | **改动登记核对**：正式 `.py`/`_agent规格/*.md`/四份规范 mtime 不得晚于 `_变更总账.md` |
| C9 | 台账最新日块日期对账（涨停/龙虎榜=`{今日}`，竞价=`{昨日}`） |
| C10 | 池外候选链核对（candidates json 非空 → 池外候选卡必须已生成） |
| C11 | **交易计划六路文件当日存在性 + schema 合法** |
| C12 | 龙虎榜黄金页专属哨兵（内部调 `lhb数据核对.py {d} --page lhb.html`） |
| C13 | 主题页同日冻结哈希（`复盘\盯盘台\.theme_page_freeze.json`）：复盘期间 **theme.html 不可改写**。★ 该冻结文件由发布链在复盘期间写入，**当前不存在**（无复盘进行中）→ 哨兵自动跳过此项 |

> ⚠ 注意：任务契约文本里出现过的 "C1-C28 / C22 / C24 / C29 / C32" 是**历史版本编号**，当前实际哨兵为 **C1~C13**。以源码为准。

**FAIL 处置**：台账/注入类 → 查第 9–12 步是否漏跑；C11 → 六路交易计划补写；C8 → 按改动三合一 SOP 补登记后重跑。
**FAIL 不许通知完成。**

### 门 3 · 发布门禁（出页链路内建）

`生成盯盘台.py` 的写入已全部走 `review_publish.build_release()`。相关门槛脚本：

| 脚本 | 参数 |
|---|---|
| `p1_p2_capability_gate.py` | `<date> [--root] [--out]` |
| `p3_production_gate.py` | `--evidence <path> [--bridge] --out <path>` |
| `stability_gate.py` | `--root <path> --out <path> [--required 5]` |
| `history_recovery_gate.py` | `--diagnosis --progress [--evidence-dir] --out` |

**发出版不可覆盖**：`_学习\{route}判断_*.json`、`题材荐票_*`、`交易计划_*`、`playbook.json` 一旦发布即冻结，禁止重跑覆盖。
禁止直接改 `复盘\盯盘台\theme.html`——`_postprocess_site` 会显式拒绝（`RuntimeError: 拒绝对现站 theme.html 后处理`）。

### 门 4 · 交付清单（交给用户时必须整包列清）

- [ ] 7 页齐全 + `archive\{今日}.html` 已建
- [ ] 五路判断 JSON + 五路 T+1 结算 + 昨日预判结算（推演命中率）
- [ ] 总审（含指派清单）+ 推演（≥5 条可证伪预测）
- [ ] playbook 预案单（买/卖/watch 票数 + 槽位占用 + 踩窗处置）
- [ ] 六路交易计划文件
- [ ] 第七账净值 + master 结算
- [ ] 专属核对 rc + 哨兵 rc + FAIL 清单及处置
- [ ] 本日 no-编造声明：哪些字段是 `null`/`unavailable`，为什么

---

## 7. 改动纪律（动生产脚本才需要）

`D:\股票数据\市场数据` 由多主体共写，**禁止静默覆盖**。

```bash
# ① 开工三查
python _协作/检查改动.py check
python _协作/改动声明.py list
tail -30 _变更总账.md

# ② 声明
python _协作/改动声明.py declare "Codex" "文件1,文件2" --purpose "改什么" --impact "影响面"

# ③ 动手 + 验证（动了哨兵/渲染/生成 → 必须重跑哨兵；新 class → 收编设计令牌）

# ④ 登记 _变更总账.md 追加一行（日期/改动/根因/验证）

# ⑤ 收尾
python _协作/改动声明.py done "Codex" --verify "哨兵rc=0 PASS"
python _协作/检查改动.py update
```

**纯执行复盘（不改脚本）也要注意**：你是**只写产物、不改脚本**。一旦你改了任何 `.py`，哨兵 C8 立刻 FAIL（除非同时补 `_变更总账.md`）。

---

## 8. 已知坑清单（实测，全部踩过）

1. ★ **`PYTHONPATH` 未清 → venv 崩溃**。每次调用必带 `env -u PYTHONPATH`。
2. ★ **生成器重跑会冲掉台账注入与五路判断注入** → 第 4 步必须严格按序；第 9/10 步的注入必须晚于第 13 步之前完成，且 `生成盯盘台.py` 只在最后跑一次。
3. ★ **交易计划断供静默空仓**：引擎读 `_学习\交易计划_{route}_{今日}.json`，文件缺 → 引擎**静默 `{}` 空仓、零告警**。必须每路都写（C11 兜底）。
4. ★ **台账脚本 `--from-data` 漏日期参数会静默跳过当日日块** → 页面停昨日，肉眼难察（C9 兜底）。
5. ★ **`复盘一致性哨兵.py` 的 C6 会抓 LLM 手写 HTML 的结构病**：div 开闭不配平、游离 `>`、`rowA` 子元素个数不对（lhb 四组件曾连续被用户抓 3–4 次，根因=强档 KPI 卡 `</div>` 错位）。
6. ★ **`复盘\盯盘台\*.html` 是生成物**：手改会被重渲染冲掉。要改内容 → 改 bodies → 重渲染。
7. ⚠ **本机不存在的脚本**（实测，契约里仍提到，遇到就跳过并如实说明）：
   `认知库打包.py`、`领域手册.py`、`作战包.py`、`因子快检.py`、`ifind查.py`。
   → 替代：`_学习\_认知库_{route}.json`、`_agent规格\*.md`；认知库蒸馏器实为 `市场数据\_认知库蒸馏_五路.py`（存在，**注意在根目录不在 `_学习\` 下**）。
8. ⚠ **`D:\股票数据\sentiment_review_v4` 目录不存在** → 流水线 9c（v4 影子重放）无法执行，据实报错跳过，**不许硬造**。
9. ⚠ **契约文本里的路径已部分陈旧**：`_学习\五路判断_{今日}\`（旧目录契约，本机不存在，勿用）、`复盘\judgment_{今日}.json`（实测 9/10 缺，权威路径是 `_学习\judgment_{今日}.json`）。
10. ★ **`_链路地图.md` 是超长行 UTF-8**，`read_file` 会误判为二进制 → 用 python 折行或 `rg` 读。
11. ★ **`_学习` 下大量 `.bak_*` 文件**是历史备份，不是当日产物，不要读错。
12. ★ Codex `bin` 目录按哈希分版本，**取最新 ≠ ls 排序最后**，必须验证目录内有 `codex.exe`（有的哈希目录只有 `rg.exe`）。
13. ★ 代理不稳（502/503/连接错误）会杀掉「最后一步输出报告」：**只要交付物已落盘就不算失败**；进程 >15 分钟无新输出且产物完整 → 可 kill，成果保留。

---

## 9. 当前系统真实状态（截至 2026-09-13，**必须知道**）

| 项 | 事实 |
|---|---|
| 最近完成的完整复盘日 | **2026-09-10**（五路/总审/推演/六路交易计划/归档 archive\20260910.html 均已存在） |
| **待补缺口** | **2026-09-11（周五）复盘未做** —— `市场数据\20260911\` 数据目录不存在，无 `judgment_20260911.json`／`总审_20260911.json`／`推演_20260911.json`／`archive\20260911.html`；仅有 `盘中\20260911\` 盘中数据与 `_学习\风险事件_20260911.json` |
| 下一交易日 | 2026-09-14（周一） |
| ★ 哨兵当前状态 | **FAIL 1 项**：`C8 改了没登记: ['review_publish.py', '复盘一致性哨兵.py', '生成盯盘台.py'] mtime 晚于 _变更总账.md` → 需按改动三合一 SOP 补登记后重跑 |
| 自动任务 | `sentiment-daily-review`（`c4d23047161f`）**已 paused**（2026-09-11 16:14 暂停）。**不要擅自启用/修改** |
| 今日（9/13）有活动改动 | 09:03 `restore_evolution_style.py`、11:00 `restore_lhb_page.py`、12:16 `review_pages.py`、12:17 `theme数据核对.py`、13:14 `review_publish.py`、13:15 `生成盯盘台.py`、13:16 `复盘一致性哨兵.py`；12:20 全站 7 页重渲 + `archive\20260910.html` 12:21 重建 → **有人在同步改这套系统，开工前先跑 `检查改动.py check`** |
| 生产目录 git | `市场数据` 非 git 仓库；镜像同步仓库在 `D:\股票数据\ding-pan仓库`（git，有 `sync YYYYMMDD watchdog自动同步` 提交） |
| 后台服务 | 盯盘台 `http://127.0.0.1:8899/`（实测在跑，root = `复盘\`）；DS额度监控 8911 |
| 历史补跑脚本 | `D:\股票数据\run_sequential_reviews_20260911.py`（按序补 20260910→20260911；其 `status.json` 停在 `blocked_after_20260910`，是**当时的**状态，9/10 后来已补齐） |

---

## 10. 你的第一个任务（建议按此顺序）

**任务 A（先做，验证你已跑通链路）**：补 **2026-09-11（周五）** 的完整复盘。

```bash
# 0. 建工作区
mkdir -p "D:/股票数据/codex_workspace/review_20260911" && cd "D:/股票数据/codex_workspace/review_20260911" && git init

# 1. 开工三查
cd "D:/股票数据/市场数据" && python _协作/检查改动.py check && tail -30 _变更总账.md

# 2. 取数（9/11 数据目录尚不存在）
cd "D:/股票数据/市场数据" && env -u PYTHONPATH "D:/股票数据/.venv312/Scripts/python.exe" 市场数据下载.py 20260911

# 3. 按第 4 节流水线逐步执行，{今日}=20260911，{昨日}=20260910
#    ★五路判断按第 5 节替代方案，每路一个 brief + 一次 codex exec

# 4. 验收门（第 6 节）—— 全部 rc=0 才算完成
```

**任务 B（顺带修）**：补登记 C8 的三笔改动（`review_publish.py` / `复盘一致性哨兵.py` / `生成盯盘台.py`）到 `_变更总账.md`，
使 `复盘一致性哨兵.py 20260910` 回到 rc=0。**补登记前先确认这三处改动的真实内容与动机**（不要凭猜测写），必要时向用户确认。

**任务 C（长期）**：9/14（周一）起按同一流水线每日执行。

---

## 11. 你的交付报告必须包含

1. 五路独立判断摘要（各路的**档位 + 置信度 + 荐票数 + 框架引用 + 主动查证记录 + 负面过滤处置**）
2. 总审对抗结论（分歧裁决 + 置信度 + 环境加权依据 + 昨日战绩验收）
3. 昨日五路战绩结算 + 昨日预判结算（推演命中率）+ 条件决断校准（兑现率）
4. 今日推演摘要
5. playbook 预案单摘要（买/卖/watch 票数 + 槽位占用 + 踩窗处置）
6. 第七账净值 / 龙虎榜双源复核 / 风险日历踩窗 / 开盘验证维样本 / 日内图谱状态
7. 情景判定、温度、仓位、**7 页是否齐全**
8. 哨兵 FAIL 清单及处置
9. **no-编造声明**：所有 `null`/`unavailable` 字段逐项说明

---

*本清单由 Hermes Agent 于 2026-09-13 现场勘察（脚本存在性、哨兵源码、cron 契约、产物 mtime 全部实测）后编写。*
