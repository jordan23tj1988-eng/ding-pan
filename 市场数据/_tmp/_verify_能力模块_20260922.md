# 验证留档：五路+概览 两块公共能力（自主拓展 / 认知迭代 · 能力进化）

**日期**：2026-09-22　**验证对象日期**：20260921　**唯一真源**：`市场数据\能力进化模块.py`
**改动登记**：`_协作/改动声明.jsonl` → `Hermes-20260922-capability`；`_变更总账.md` #199（含同日补记）；`_链路地图.md` v1.12；`cockpit_versions.json` meta.last_change=199

---

## 1. 真实性：现站七页能力块 = 当日唯一真源（逐字节）

`能力进化模块.verify_page(live_html, route, 生产根, '20260921')`：

| 页面 | 能力块数 | evolution 末两段标题 | 旧模块残留 | verify_page |
|---|---|---|---|---|
| index（概览） | 2 | 七 自主拓展 · 能力进化 / 八 认知迭代 · 能力进化 | 无 | 一致 |
| cycle（周期） | 2 | 六 / 七 | 无 | 一致 |
| auction（竞价） | 2 | 五 / 六 | 无 | 一致 |
| lhb（席位） | 2 | 五 / 六 | 无 | 一致 |
| theme（题材） | 2 | 四 / 五 | 无 | 一致 |
| logic（逻辑） | 2 | 六 / 七 | 仅 claim 正文一处「自主深挖」（该路 claim「旧版原文补充」，非模块残留） | 一致 |
| limitup（涨停） | 2 | 五 / 六 | 无 | 一致 |

- 编号 = 各页业务段数顺延（index 6 业务段→七/八；theme 3 业务段→四/五），与 `CAPABILITY_BUSINESS_SECTIONS` 一致，业务段编号零漂移。
- 线上静态服务（127.0.0.1:8899，根=`D:\股票数据\市场数据`）实测七页均返回 2 个 `<section class="evolution">`。

## 2. 正式发布链（真实生产入口，非临时脚本）

`生成盯盘台.py 20260921`（= 每日复盘 prompt 第 13 步同一条命令）：

- P2 生产审计通过 → `build_release(publish=True)` → `status=pass`、`published=True`、`errors=[]`
- 逐路检查：cycle / auction / lhb / theme / limitup / logic / consistency / browser **8/8 pass**
- 后处理：`标准能力模块(七页) index(七/八) cycle(六/七) auction(五/六,清旧2) lhb(五/六,清旧2) theme(四/五) logic(六/七,清旧2) limitup(五/六,清旧2)`；能力数据根 = 本次 build **冻结 inputs**（与门禁/哨兵复算同源）
- 冻结输入面已含能力面：`能力进化快照_20260918.json` + `能力进化库_自主拓展.json` + `能力进化库_认知迭代.json` + `主题页能力进化模板.html`
- 到站：7 主页面复制 + 存档/历史更新；theme 冻结守卫 before==after（`68191f3fecd3…`）
- **现站 = 发布候选 = 逐字节一致**（七页 sha256 全同）

## 3. 幂等性

同一日、同一最终代码的三次独立 rev-3 build（`67604a00` / `8edbc808` / `1e9621a9`）与正式 release `73e0df48` 的七页 **sha256 完全一致** → 后处理确定性、无随机/时间戳污染。

## 4. 负面注入（在真实发布候选 stage 上执行，非 fixture）

| 注射 | 预期 | 实得 |
|---|---|---|
| 删除 index 一块能力模块 | 阻断 | `validate_capability_blocks` 报缺陷 ✓（该 build 已 FAIL 存档 `20260921-3-8edbc…`） |
| 塞入旧标题「我的认知迭代」 | 阻断 | 报「旧模块残留」✓ |
| 块内字节篡改（改一个数字） | 阻断 | 报「字节与当日唯一真源不一致」✓ |

历史缺陷回放：修复前 C14 对七页全部报 `能力模块不合契约`；修复后 C14 通过（哨兵 `20260921` 现站结论：**全过 0 警告**）。

## 5. 回归测试与单元测试

- `tests/test_cycle_display_contract.py`（重写为两块公共能力统一契约）：**6 passed**
- `tests/test_review_pages_reco_wiring.py::test_p1_revalidation_matches_dom_for_reco`：**passed**（本次改动曾引入回归，已修）
- 全量 `pytest tests/ -q -p no:randomly`：见同目录 `pytest_final_20260922.txt`（3 例既有 RED：cycle 机器锚 VOTEBOARD→MACHVOTE 改名后的旧断言 ×2、`review_pages._render` root=None fixture ×1；已用「换回原始 review_publish.py 复跑」证明与本次改动无关）

## 6. 下次复盘不再出问题的机制（四条防线）

1. **唯一真源**：`能力进化模块.py` 从当日能力库直出，禁止页面搬运字节；缺数据=0/空提示，不补造。
2. **生成期自检**：`生成盯盘台.py::_sync_capability_blocks` 注入后立即 `verify_page`（与注入同数据根），失败即报。
3. **发布门禁**：`review_publish.validate_capability_blocks(frozen, stage, d)` 在发布前逐路阻断（缺块/旧残留/编号漂移/位置/字节不一致）。
4. **现站哨兵 C14**：`复盘一致性哨兵.py` 每日随 C 系列复核，机器卡缺席之外的静默缺块也能抓到。

**注**：`intraday`（盘中作战台）不在本契约内，走 warboard 链路。
