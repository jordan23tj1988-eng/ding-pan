# -*- coding: utf-8 -*-
import json, io, os
base = r'D:\股票数据\市场数据\_学习'
d = json.load(io.open(os.path.join(base, '涨停质量荐票_20260818.json'), encoding='utf-8'))
det = d['明细']
want = {'600127', '600613', '002820', '603395', '002041', '000998', '600598'}
for x in det:
    if x['代码'] in want:
        print(x['代码'], x['名称'], '连板', x['连板'], '首封', x.get('首封时间'), '开板', x.get('开板次数'),
              '封单比', x.get('封单比'), '封单额', x.get('封单额'), '抓龙率', x['抓龙率'], '命中', x['命中数'],
              '执1', x['预测执1胜率'], x['预测执1均涨'], '执2', x['预测执2胜率'], x['预测执2均涨'],
              '大方向', x.get('大方向'), '催化', x.get('催化', '')[:40])
# 神奇制药 题材环节
