# 全链路体检报告 · 2026-07-07

> 目的：确认所有子agent、自动化脚本、页面之间的联动是否正常。实测非推测。

## ✅ 通（正常）
1. **数据流产物齐全**：07-06/07-07 两天 取数→分析→训练→校准 全环产物在（zt/dt/zb/strong_pool、lhb、summary、analysis、concept_rank）。
2. **数据无串档**：两天 md5 全不同、行数 64 vs 33，真不同数据（此前"字节数相同"是巧合，已排除）。
3. **脚本语法**：16 个脚本中 15 个 py_compile 通过。
4. **新盯盘台页面内部联动**：6 页(index/cycle/theme/lhb/auction/history)+存档 链接全英文名、目标齐全、无中文文件名引用、乱码已清 → 页面互跳正常。
5. **旧出页链路**：生成复盘HTML.py → 复盘/latest.html + 训练页，自洽。
6. **定时任务在跑**：mikai-daily-review、mikai-morning-auction 均 enabled，07-07 当天都执行过。

## ⚠️ 断 / 待接
1. **★取数脚本 市场数据下载.py 当前视图被截断**（130行，停在 `z = retry(lambda:` 半句）。
   - 影响：链路起点，现视图下跑不了。
   - 存疑：真截断(长行写入bug) or 挂载快照残缺视图？**未确认前不覆盖**（避免盖掉磁盘上可能完好的原版）。
   - 待办：用户在真机看该文件实际行数 / 或重启会话刷新挂载，再定是否重建。
   - 注：07-07 数据是它今晨 08:31 截断前跑出来的，说明原版是完好的。
2. **★新盯盘台无脚本自动生成**：目前是手动构建。定时任务仍生成"旧单页"。→ 新旧两套页面并存，新盯盘台未进自动链路。
3. **定时任务仍是旧设计**：18:30 / 9:32、旧3-agent链、旧输出；未升级到 v0.3 的 9:30/18:00 + 9-agent + 新盯盘台。
4. **推理型环节未自动化**：认知/推演/校准归因/命门深读 目前由 Claude 手工做，尚未固化成定时任务里自动派的子agent。
5. akshare 沙箱重置需重装（已知，非故障）。

## 结论
**数据流与页面内部联动正常；但取数脚本当前视图损坏(待确认真伪)、新盯盘台与新链路尚未接入自动化。**
大框架成立；要让"每天自动更新"真正跑起来，需接：①确认/修复取数脚本 ②新盯盘台生成脚本化 ③定时任务升级到新链路+推理型子agent。
